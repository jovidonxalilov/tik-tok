enum AppStatus {
  initial,
  loading,
  success,
  error,
}

enum CameraStatus {
  initial,
  ready,
  recording,
  captured,
  uploading,
  uploaded,
  error,
}

enum ProfileStatus {
  initial,
  loading,
  loaded,
  error,
}